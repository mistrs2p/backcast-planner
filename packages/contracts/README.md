# packages/contracts — Shared Contracts

Shared API contracts and schemas consumed by the backend (`apps/api`) and the
frontend (`apps/web`).

Per `docs/10-TECHNICAL-ARCHITECTURE.md`, the API exposes use cases and
generated contracts. The contract format and code generation tooling are
defined by TASK-010 (Shared contracts foundation); this package is
intentionally a placeholder until then.
