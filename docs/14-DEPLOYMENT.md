# Deployment

Environments: local → development → staging → production.

Pipeline: validate → build → migrate safely → deploy → smoke test → health check.

Production requirements: secrets management, logging, metrics, traces, backup, restore test, rollback strategy.
